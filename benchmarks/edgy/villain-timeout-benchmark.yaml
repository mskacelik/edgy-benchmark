name: villain-timeout-blast-ramp-benchmark-edgy

http:
  host: http://localhost:8083
  sharedConnections: 20000

phases:
  - blastPhase:
      increasingRate:
        initialUsersPerSec: 1
        targetUsersPerSec: 500
        duration: 2m
        scenario:
          initialSequences:
            - unstableTimeout:
                - httpRequest:
                    GET: /edgy/villain-unstable-timeout-guard
