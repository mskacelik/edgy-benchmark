name: villain-predicate-blast-ramp-benchmark-scg

http:
  host: http://localhost:8084
  sharedConnections: 20000

phases:
  - blastPhase:
      increasingRate:
        initialUsersPerSec: 1
        targetUsersPerSec: 10000
        duration: 2m
        scenario:
          initialSequences:
            - isVillain:
                - httpRequest:
                    GET: /scg/am-i-villain
                    headers:
                      X-Is-Villain: "yes"
#            - isNotVillain:
#                - httpRequest:
#                    GET: /scg/am-i-villain
