name: villain-persistenceless-blast-ramp-benchmark-scg

http:
  host: http://localhost:8084
  sharedConnections: 20000

phases:
  - blastPhase:
      increasingRate:
        initialUsersPerSec: 1
        targetUsersPerSec: 20000
        duration: 2m
        scenario:
          initialSequences:
            - amIVillain:
                - httpRequest:
                    GET: /scg/am-i-villain
